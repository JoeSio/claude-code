{
  "name": "Claude Code Sandbox",
  "build": {
    "dockerfile": "Dockerfile",
    "args": {
      "TZ": "${localEnv:TZ:Asia/Shanghai}",
      "CLAUDE_CODE_VERSION": "latest",
      "CODEX_VERSION": "latest",
      "QWEN_CODE_VERSION": "latest",
      "KIMI_CLI_VERSION": "latest",
      "GIT_DELTA_VERSION": "0.18.2"
    }
  },
  "runArgs": [
    "--network",
    "container:myvpn"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "anthropic.claude-code",
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "eamodio.gitlens"
      ],
      "settings": {
        "editor.formatOnSave": true,
        "editor.defaultFormatter": "esbenp.prettier-vscode",
        "editor.codeActionsOnSave": {
          "source.fixAll.eslint": "explicit"
        },
        "terminal.integrated.defaultProfile.linux": "zsh",
        "terminal.integrated.profiles.linux": {
          "bash": {
            "path": "bash",
            "icon": "terminal-bash"
          },
          "zsh": {
            "path": "zsh"
          }
        }
      }
    }
  },
  "remoteUser": "codespace",
  "mounts": [
    "source=claude-code-bashhistory-${devcontainerId},target=/commandhistory,type=volume",
    "source=claude-code-config-${devcontainerId},target=/home/codespace/.claude,type=volume",
    "source=codex-config-${devcontainerId},target=/home/codespace/.codex,type=volume",
    "source=qwen-code-config-${devcontainerId},target=/home/codespace/.qwen,type=volume",
    "source=kimi-config-${devcontainerId},target=/home/codespace/.kimi,type=volume"
  ],
  "containerEnv": {
    "NODE_OPTIONS": "--max-old-space-size=4096",
    "CLAUDE_CONFIG_DIR": "/home/codespace/.claude",
    "CODEX_HOME": "/home/codespace/.codex",
    "KIMI_SHARE_DIR": "/home/codespace/.kimi",
    "POWERLEVEL9K_DISABLE_GITSTATUS": "true"
  },
  "workspaceMount": "source=claude-code-workspace,target=/myapp,type=volume",
  "workspaceFolder": "/myapp",
  "postCreateCommand": "sudo chown -R codespace:codespace /myapp 2>/dev/null || true",
  "postStartCommand": "sudo chown -R codespace:codespace /myapp 2>/dev/null || true; echo 'Using Gluetun VPN network (container: myvpn)'",
  "waitFor": "postStartCommand"
}
